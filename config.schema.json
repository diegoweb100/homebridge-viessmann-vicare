{
  "pluginAlias": "ViessmannPlatform",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for Viessmann heating systems with full control capabilities including boiler, DHW, and heating circuits.",
  "footerDisplay": "For help and support visit: [GitHub Repository](https://github.com/your-username/homebridge-viessmann-control)",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Viessmann",
        "required": true
      },
      "clientId": {
        "title": "Client ID",
        "type": "string",
        "required": true,
        "description": "Your Viessmann API Client ID"
      },
      "clientSecret": {
        "title": "Client Secret",
        "type": "string",
        "required": false,
        "description": "Your Viessmann API Client Secret (optional for public clients)"
      },
      "username": {
        "title": "Username",
        "type": "string",
        "required": true,
        "description": "Your Viessmann ViCare account username/email"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "Your Viessmann ViCare account password"
      },
      "authMethod": {
        "title": "Authentication Method",
        "type": "string",
        "enum": ["auto", "manual"],
        "default": "auto",
        "description": "Choose authentication method: 'auto' for automatic OAuth flow, 'manual' for manual token configuration"
      },
      "hostIp": {
        "title": "Host IP Address",
        "type": "string",
        "required": false,
        "description": "IP address for OAuth redirect (auto-detected if not specified). Use the IP where Homebridge is running."
      },
      "redirectPort": {
        "title": "OAuth Redirect Port",
        "type": "integer",
        "default": 4200,
        "minimum": 1024,
        "maximum": 65535,
        "description": "Port for OAuth callback server (default: 4200)"
      },
      "accessToken": {
        "title": "Access Token (Manual Auth)",
        "type": "string",
        "required": false,
        "description": "Manual access token (only required if authMethod is 'manual')"
      },
      "refreshToken": {
        "title": "Refresh Token (Manual Auth)",
        "type": "string",
        "required": false,
        "description": "Manual refresh token (only required if authMethod is 'manual')"
      },
      "refreshInterval": {
        "title": "Refresh Interval",
        "type": "integer",
        "default": 60000,
        "minimum": 30000,
        "maximum": 300000,
        "description": "How often to update device data (in milliseconds). Default: 60000 (1 minute)"
      },
      "debug": {
        "title": "Enable Debug Logging",
        "type": "boolean",
        "default": false,
        "description": "Enable verbose debug logging for troubleshooting"
      }
    },
    "required": [
      "name",
      "clientId",
      "username",
      "password"
    ]
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Account Settings",
      "description": "Configure your Viessmann ViCare account credentials",
      "expandable": true,
      "expanded": true,
      "items": [
        "name",
        "clientId",
        "clientSecret",
        "username",
        "password"
      ]
    },
    {
      "type": "fieldset",
      "title": "Authentication Method",
      "description": "Choose how to authenticate with Viessmann API",
      "expandable": true,
      "expanded": true,
      "items": [
        "authMethod",
        "hostIp", 
        "redirectPort"
      ]
    },
    {
      "type": "fieldset",
      "title": "Manual Authentication (Advanced)",
      "description": "Only required if authMethod is set to 'manual'",
      "expandable": true,
      "expanded": false,
      "items": [
        "accessToken",
        "refreshToken"
      ]
    },
    {
      "type": "fieldset",
      "title": "Advanced Settings",
      "description": "Optional advanced configuration",
      "expandable": true,
      "expanded": false,
      "items": [
        "refreshInterval",
        "debug"
      ]
    }
  ]
}